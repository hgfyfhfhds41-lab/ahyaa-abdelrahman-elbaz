# التعريف الشامل بتطبيق «أحياء مع عبدالرحمن الباز»

## 1. بطاقة تعريف التطبيق

**اسم التطبيق:** أحياء مع عبدالرحمن الباز  
**نوع التطبيق:** تطبيق تعليمي للهواتف، موجّه أساسًا إلى Android  ️
**مجاله:** تعليم مادة الأحياء من خلال محتوى قناة الأستاذ عبدالرحمن الباز على YouTube.  
**لغة الواجهة:** العربية.  
**اتجاه الواجهة:** من اليمين إلى اليسار RTL.  
**الوضع البصري:** Dark Theme ثابت، مستوحى من المرجع التصميمي المعتمد.  
**مصدر المحتوى:** قناة YouTube الرسمية المرتبطة بالمعرّف `UCZJdmjp4Mt-wU7miDGMEOuA`.  
**هوية التطبيق:** التطبيق واجهة تعليمية مستقلة ومنظمة؛ ليس نسخة من تطبيق YouTube ولا يعيد استضافة فيديوهات YouTube.

> **الفكرة الأساسية:** توفير نقطة وصول تعليمية مرتبة لطلاب الأحياء، تعرض اسم القناة وصورتها وفيديوهاتها وقوائم تشغيلها، وتسمح للمستخدم باختيار فيديو ومشاهدته داخل واجهة التطبيق باستخدام مشغل YouTube المضمّن الرسمي.

## 2. لماذا أُنشئ التطبيق؟

أُنشئ التطبيق لتسهيل وصول الطالب إلى شرح الأحياء في مكان واحد بدل البحث اليدوي داخل YouTube. فهو يحوّل محتوى القناة إلى تجربة تعليمية مرتبة: شاشة رئيسية تلخص المحتوى، شاشة للفيديوهات، شاشة لقوائم التشغيل، بحث داخل فيديوهات القناة، وشاشة تفاصيل لكل فيديو.

التطبيق مناسب للطلاب الذين يريدون متابعة الدروس بالترتيب، الرجوع إلى محاضرات سابقة، تصفح قوائم المنهج، ومعرفة معلومات الفيديو قبل تشغيله. كما أنه مناسب لولي الأمر أو المعلم الذي يريد الوصول السريع إلى مصدر تعليمي محدد دون إنشاء حساب داخل التطبيق.

## 3. ما الذي يفعله التطبيق؟

عند تشغيل التطبيق، يحاول الاتصال بخدمة YouTube Data API v3 من داخل التطبيق مباشرة. إذا نجح الاتصال، يعرض بيانات القناة الحقيقية، مثل اسمها وصورتها ووصفها وبعض الإحصائيات العامة المتاحة، ثم يجلب الفيديوهات العامة وقوائم التشغيل والصور المصغرة والعناوين والتواريخ والبيانات التي تسمح بها API.

يمكن للمستخدم تحديث المحتوى يدويًا عبر زر «إعادة المحاولة» أو السحب للتحديث حسب الشاشة. وعند اختيار فيديو، ينتقل إلى صفحة التفاصيل التي تعرض بيانات الفيديو الحقيقية والوصف القابل للطي، ثم تجهز مشغل YouTube داخل التطبيق. لا تظهر بيانات تجريبية لتعويض فشل الاتصال؛ عند فشل API تظهر رسالة عربية واضحة وزر لإعادة المحاولة.

## 4. مكونات التطبيق الرئيسية

| المكوّن | وظيفته |
|---|---|
| الشاشة الرئيسية | عرض تعريف القناة، أحدث الفيديوهات، وقوائم التشغيل مع حالات التحميل والخطأ والتحديث. |
| شاشة الفيديوهات | عرض فيديوهات القناة الحقيقية، مع البحث والتحديث والصور المصغرة. |
| شاشة تفاصيل الفيديو | عرض العنوان والوصف والتاريخ والمدة والمشاهدات وVideo ID وحالة التشغيل. |
| مشغل YouTube | تشغيل الفيديو داخل التطبيق عبر YouTube IFrame Player API داخل WebView على Android. |
| شاشة قوائم التشغيل | عرض Playlists الحقيقية وعدد عناصرها وصورها وعناوينها. |
| شاشة تفاصيل Playlist | عرض عناصر القائمة الحقيقية بالترتيب الذي توفره YouTube، مع الانتقال للفيديو السابق أو التالي عند توفره. |
| شاشة البحث | البحث داخل فيديوهات القناة التي تم جلبها من المصدر الحقيقي، دون نتائج وهمية. |
| شاشة التنزيلات | واجهة محفوظات/تنزيلات ضمن التصميم، لكنها لا تنزل فيديوهات YouTube ولا تحفظ ملفات MP4. |
| شاشة المزيد | عرض معلومات التطبيق وتفضيلات العرض ومعلومات حالة المصدر دون روابط YouTube خارجية. |
| شاشة إعدادات المصدر | شاشة داخلية للحالة والإعدادات، ولا تعرض زر «فتح القناة الرسمية» أو أي رابط خارجي من تصميم التطبيق. |
| شريط التنقل السفلي | خمس وجهات: الرئيسية، الفيديوهات، البحث، التنزيلات، والمزيد. |

## 5. وصف الواجهة والتصميم

التصميم الحالي مبني على مرجع بصري لهوية تعليمية داكنة. الخلفية كحلية شديدة العمق، والبطاقات بلون أفتح قليلًا مع حدود رقيقة، بينما يستخدم اللون الفيروزي للعناصر التفاعلية والعناوين الثانوية وحالة التبويب النشط. النص الأساسي أبيض أو قريب من الأبيض، والنص الثانوي رمادي هادئ.

في أعلى الشاشة يظهر اسم التطبيق مع أزرار مختصرة مثل البحث والتنبيهات عند توفرها. أسفل ذلك توجد منطقة تعريفية بالقناة تحتوي على اسم «أحياء مع الباز»، صورة الأستاذ، اسم القناة، والمعرّف العام. بعد منطقة التعريف تأتي أقسام المحتوى مرتبة رأسيًا، مع عناوين واضحة وروابط داخلية مثل «عرض الكل» عندما تكون الوجهة داخل التطبيق.

بطاقات الفيديو تعتمد على الصورة المصغرة كعنصر بصري رئيسي، وتعرض العنوان والبيانات المتاحة دون ازدحام. أما بطاقات قوائم التشغيل فتظهر أفقيًا، لتناسب الاستخدام بيد واحدة والتمرير الجانبي. شريط التنقل السفلي ثابت ويحتوي على خمس وجهات واضحة، مع تمييز الوجهة الحالية باللون الفيروزي.

الواجهة لا تتحول إلى نسخة من YouTube؛ فهي تحافظ على هوية التطبيق التعليمية وعلى بنية التطبيق الخاصة، بينما يظهر داخل المشغل فقط ما يفرضه مشغل YouTube المضمّن من عناصر أو إفصاحات.

## 6. الشاشات وتدفقات الاستخدام

### فتح التطبيق

يفتح المستخدم التطبيق فتبدأ عملية جلب بيانات القناة والمحتوى الحقيقي. أثناء الطلب يظهر Loading مناسب. عند النجاح تظهر القناة والفيديوهات والقوائم. عند الفشل تظهر رسالة عربية تشرح أن البيانات لم تُجلب، مع زر «إعادة المحاولة» دون وضع بطاقات وهمية.

### مشاهدة فيديو

يختار المستخدم بطاقة فيديو من الرئيسية أو شاشة الفيديوهات أو قائمة تشغيل. يفتح التطبيق شاشة التفاصيل، ثم يتحقق من Video ID ومن نتيجة `embeddable` التي توفرها بيانات YouTube. إذا كان الفيديو قابلًا للتضمين، يظهر مشغل IFrame داخل صفحة التطبيق. وإذا لم يكن قابلًا للتضمين، تظهر رسالة عربية واضحة ولا يحاول التطبيق تجاوز القيد ولا يفتح المتصفح تلقائيًا.

### تصفح Playlist

يفتح المستخدم شاشة قوائم التشغيل ويختار قائمة حقيقية. يجلب التطبيق عناصر القائمة بالترتيب، مع pagination عند الحاجة. يمكن للمستخدم فتح أي عنصر، ثم استخدام السابق أو التالي عندما يكون سياق القائمة متاحًا. لا تُنشأ اقتراحات وهمية خارج محتوى القناة أو القائمة.

### البحث والتحديث

يكتب المستخدم كلمة بحث في شاشة البحث، فيقوم التطبيق بتصفية المحتوى الحقيقي المتاح من القناة. ويمكنه إعادة الطلب عبر Refresh أو زر إعادة المحاولة عند وجود خطأ في الشبكة أو API.

## 7. مصدر البيانات وتكامل YouTube

يستخدم التطبيق **YouTube Data API v3** لجلب بيانات القناة والفيديوهات وقوائم التشغيل والصور المصغرة والإحصائيات العامة المتاحة. يعتمد جلب فيديوهات القناة على Uploads Playlist الرسمي للقناة بدل البحث العام، ويستخدم pagination لجلب الصفحات المتتالية. كما تُقسّم طلبات تفاصيل الفيديو إلى دفعات لا تتجاوز الحد الرسمي للمعرّفات في الطلب الواحد.

المعرّف الثابت للقناة هو:

`UCZJdmjp4Mt-wU7miDGMEOuA`

يوجد عميل API منظم داخل المشروع يحوّل استجابات YouTube إلى أنواع بيانات يستخدمها التطبيق. ويتعامل العميل مع حالات HTTP والأخطاء ورسائل API، ويحوّل الأسباب المهمة إلى رسائل عربية مفهومة مثل المفتاح غير الصالح، عدم تفعيل API، تجاوز الحصة، أو انقطاع الشبكة.

## 8. مشغل الفيديو

يستخدم التطبيق YouTube IFrame Player API داخل `react-native-webview` لمسار Android. هذه الطريقة تُبقي الفيديو داخل واجهة التطبيق وتستخدم المشغل الرسمي المضمّن بدل تحميل الفيديو أو إعادة استضافته. حزمة `react-native-webview` مطلوبة للمسار الأصلي Android، ولا تعمل في Web Preview؛ لذلك يعرض التطبيق في المعاينة رسالة عربية تفيد بأن تشغيل الفيديو يحتاج نسخة Android.

حالات المشغل التي صُممت لها الواجهة هي: التحميل، الجاهزية، التشغيل، الإيقاف المؤقت، التخزين المؤقت، الخطأ، عدم توفر الفيديو، عدم قابلية التضمين، وعدم توفر الشبكة. لا يفتح التطبيق YouTube أو المتصفح الخارجي تلقائيًا كحل بديل.

## 9. الأمان والأسرار

يحتاج التطبيق إلى متغير سري باسم `YOUTUBE_API_KEY`. لا يجب كتابة قيمة المفتاح في ملفات المصدر أو رفعها إلى GitHub أو وضعها في التقرير أو السجل. يُضاف المفتاح في إعدادات بيئة البناء الخارجية، ثم يستخدمه إعداد Expo أثناء بناء التطبيق.

لأن الخيار المعتمد هو الاتصال المباشر من تطبيق Android، فإن المفتاح يصبح جزءًا من إعداد التطبيق المبني ويمكن نظريًا استخراجه من APK؛ لذلك يجب تقليل المخاطر بتقييده في Google Cloud على النحو التالي:

| القيد | القيمة المطلوبة |
|---|---|
| نوع التطبيق | Android application |
| اسم الحزمة | `com.app.ahyaaabdelrahmanelbaz` |
| شهادة التوقيع | SHA-1 للشهادة التي وقعت APK الموزع فعليًا |
| تقييد API | YouTube Data API v3 فقط |

إذا اختلفت شهادة APK الاختباري عن شهادة APK النهائي، يجب استخدام بصمة الشهادة المطابقة للنسخة المثبتة. عدم تطابق اسم الحزمة أو SHA-1 قد يجعل Google ترفض الطلب رغم صحة المفتاح.

## 10. ما لا يفعله التطبيق

التطبيق لا يحتوي على OAuth أو تسجيل دخول Google أو حسابات مستخدمين أو تعليقات أو إعجابات أو اشتراك إجباري. ولا يستخدم Firebase أو Supabase أو Backend خاصًا أو قاعدة بيانات أو تخزينًا للفيديوهات. ولا ينزّل فيديوهات YouTube ولا ملفات MP4 ولا يعيد استضافة الفيديوهات ولا يحاول تجاوز قيود `embeddable`.

كما أن التطبيق لا يضيف أزرارًا تفتح قناة YouTube أو صفحة الفيديو في متصفح خارجي. التشغيل المقصود يتم داخل مشغل YouTube المضمّن، مع احترام أي عناصر أو إفصاحات يفرضها YouTube داخل المشغل نفسه.

## 11. التقنية وبنية المشروع

المشروع مبني باستخدام Expo SDK 54 وReact Native وTypeScript وExpo Router. يستخدم NativeWind وملفات الثيم لتوحيد الألوان، و`react-native-safe-area-context` لحماية المحتوى من النتوءات وشريط النظام، و`react-native-webview` لتشغيل IFrame على Android.

التقسيم المنطقي للمشروع يتضمن مجلدات للشاشات، ومكونات واجهة مشتركة، وثوابت الثيم، وعميل YouTube، واختبارات Vitest، وإعداد Expo. لا توجد حاجة إلى خادم خاص للتطبيق الحالي لأن طلبات YouTube مباشرة من الجهاز وفق الخيار المعتمد.

## 12. الاختبارات وحالة المشروع

تشمل الاختبارات البرمجية فحص TypeScript وVitest وLint، والتحقق من أن Channel ID ثابت، وأن المفتاح لا يُطبع في السجلات، وأن البيانات الوهمية لا تُستخدم لتغطية فشل API. كما تمت مراجعة عدم وجود روابط YouTube خارجية أضافها التطبيق.

اختبار Android الفعلي يجب أن يتم على APK مثبت على جهاز Android حقيقي أو محاكي متوافق. لا يكفي Web Preview لاختبار WebView أو Play/Pause/Buffering. عند الاختبار الفعلي يجب التأكد من فتح فيديو قابل للتضمين، ظهور التفاصيل، تشغيل المشغل، الإيقاف والاستئناف، التخزين المؤقت، الانتقال داخل Playlist، وحالات عدم التوفر والشبكة.

## 13. البناء والنشر

يحتوي المشروع على ملف `GITHUB_BUILD.md` يشرح إعداد البناء الخارجي. المستودع المرفوع إلى GitHub خاص، ويحتوي على الكود والأصول والتعليمات غير السرية. لا يحتوي على قيمة `YOUTUBE_API_KEY`.

لإنشاء APK خارجي، تُسحب نسخة فرع `main`، وتُثبت الاعتماديات، ويُضاف السر `YOUTUBE_API_KEY` في بيئة البناء، ثم يُبنى التطبيق باستخدام أداة Expo/EAS أو منصة البناء المعتمدة. بعد البناء يجب حذف النسخة القديمة من الجهاز وتثبيت APK الجديد، ثم اختبار الشبكة والمحتوى والمشغل.

## 14. المشاكل المحتملة ومعانيها

| الرسالة أو الحالة | المعنى المحتمل | الإجراء |
|---|---|---|
| مفتاح YouTube غير صالح | المفتاح غير موجود في APK أو Placeholder أو القيمة قديمة | أعد البناء بعد إضافة `YOUTUBE_API_KEY` في بيئة البناء. |
| رفض المفتاح رغم صحته | اسم الحزمة أو SHA-1 لا يطابق APK أو API غير مفعلة | راجع Google Cloud وقيود Android وشهادة التوقيع. |
| لا توجد فيديوهات | فشل طلب القناة أو Uploads Playlist أو الشبكة | راجع رسالة API العربية واضغط إعادة المحاولة. |
| WebView لا يدعم المنصة | التطبيق يعمل في Web Preview | اختبر المشغل داخل Android فقط. |
| الفيديو غير قابل للتضمين | YouTube يمنع تضمينه | اعرض الحالة داخل التطبيق ولا تتجاوز القيد. |
| تجاوز الحصة | مشروع Google تجاوز حصة YouTube API | راجع الحصة من Google Cloud ولا تستخدم مفتاحًا آخر. |

## 15. الخلاصة

«أحياء مع عبدالرحمن الباز» هو تطبيق تعليمي عربي مستقل يسهّل تصفح محتوى قناة الأحياء الرسمية وعرضه داخل تجربة داكنة مرتبة ومناسبة للهاتف. يعتمد التطبيق على بيانات YouTube الحقيقية، ويحافظ على تصميم تعليمي خاص، ويعرض حالات الفشل بصدق بدل البيانات الوهمية. التقنية الحالية تركز على Android، وتستخدم YouTube Data API لجلب المحتوى وYouTube IFrame داخل WebView لتشغيل الفيديو، مع الالتزام بعدم إنشاء حسابات أو Backend أو قاعدة بيانات أو Downloader.

## مراجع رسمية

[1]: https://developers.google.com/youtube/v3/docs YouTube Data API v3 Documentation  
[2]: https://developers.google.com/youtube/iframe_api_reference YouTube IFrame Player API Reference  
[3]: https://developers.google.com/youtube/android YouTube for Android Documentation  
[4]: https://docs.expo.dev/versions/latest/sdk/webview/ Expo WebView Documentation

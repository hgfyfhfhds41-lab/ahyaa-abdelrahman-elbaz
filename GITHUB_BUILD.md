# بناء تطبيق أحياء مع عبدالرحمن الباز من GitHub

## الملفات المرفوعة

هذا المستودع يحتوي على كود Expo والواجهات والأصول وعميل YouTube Data API ومشغل YouTube IFrame داخل `react-native-webview`. لا يحتوي المستودع على مفاتيح API أو كلمات مرور أو ملفات أسرار.

## السر المطلوب

يجب إنشاء متغير سري باسم `YOUTUBE_API_KEY` في بيئة البناء الخارجية. لا تضع المفتاح في ملف مصدر أو في `app.config.ts` مباشرة، ولا ترفعه إلى GitHub. أثناء بناء Expo، يقرأ `app.config.ts` السر ويضعه في إعداد التطبيق، ثم يقرأه التطبيق من Expo Constants وقت التشغيل.

## تقييد مفتاح Google

فعّل **YouTube Data API v3** في Google Cloud Console. قيّد المفتاح بتطبيق Android باستخدام:

| الحقل | القيمة |
|---|---|
| اسم الحزمة | `com.app.ahyaaabdelrahmanelbaz` |
| SHA-1 | بصمة شهادة التوقيع التي ستوقّع APK الموزّع فعليًا |
| API restriction | YouTube Data API v3 فقط |

إذا اختلفت شهادة التوقيع بين نسخة الاختبار ونسخة التوزيع، أضف بصمة كل شهادة مستخدمة أو ابنِ النسخة بالشهادة التي قُيّد بها المفتاح.

## البناء الخارجي

ثبّت الاعتماديات باستخدام `pnpm install` ثم ابنِ المشروع بأداة Expo/EAS التي تستخدمها. قبل بدء البناء، أضف `YOUTUBE_API_KEY` كسر في إعدادات مشروع البناء، وليس كمتغير عادي ظاهر في السجل. استخدم ملف `app.config.ts` الموجود كما هو، ولا تستبدل Channel ID التالي:

`UCZJdmjp4Mt-wU7miDGMEOuA`

## ملاحظات مهمة

التكامل الحالي لا يستخدم Backend أو Firebase أو Supabase أو قاعدة بيانات أو OAuth. لا يوجد تنزيل MP4 أو إعادة استضافة للفيديوهات. تشغيل الفيديو يتم داخل التطبيق عبر YouTube IFrame، بينما `react-native-webview` يحتاج Android ولا يعمل داخل Web Preview.

بعد البناء، احذف النسخة القديمة من الجهاز وثبّت APK الجديد للتأكد من أن إعدادات Expo المضمّنة حديثة وأن APK موقّع بالشهادة التي قُيّد بها المفتاح.

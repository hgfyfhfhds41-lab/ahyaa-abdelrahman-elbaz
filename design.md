# خطة تصميم تطبيق «أحياء مع عبدالرحمن الباز»

## التوجه العام

التطبيق Android عربي بالكامل، يعمل في الوضع العمودي 9:16، ويعتمد على تخطيط RTL واستخدام مريح بيد واحدة. الهوية البصرية تعليمية وشبابية وهادئة، لكنها ليست نسخة من YouTube؛ التطبيق يقدّم «غرفة مذاكرة» خاصة بالمحتوى مع إبقاء YouTube مصدر البيانات والتشغيل.

يستخدم التصميم مبادئ Apple Human Interface Guidelines من حيث وضوح التسلسل البصري، المساحات المريحة، الأزرار الكبيرة القابلة للمس، التغذية الراجعة عند الضغط، واحترام مناطق الأمان. لا توجد شاشة تسجيل دخول أو قاعدة بيانات أو تخزين لمحتوى القناة في هذه المرحلة.

## قائمة الشاشات

| الشاشة | المحتوى والوظيفة |
|---|---|
| الرئيسية | هوية القناة، نبذة تعريفية، أحدث الفيديوهات، قوائم التشغيل، قسم المنهج، البحث، وتحديث المحتوى. عند عدم توفر بيانات YouTube تظهر حالة واضحة بدل بيانات وهمية. |
| إعدادات المصدر | شاشة داخلية لتحديد/عرض Channel Handle وChannel ID وبيئة API، مع توضيح أن القيم الرسمية فقط تُستخدم. لا تُعرض مفاتيح سرية داخل الواجهة. |
| نتائج البحث | هيكل جاهز لعرض نتائج البحث القادمة من YouTube فقط، مع حالة فارغة مفهومة في هذه المرحلة إذا لم تُضبط بيانات الاتصال. |
| تفاصيل الفيديو | هيكل التنقل المستقبلي لعرض بيانات الفيديو ثم فتح رابط YouTube الرسمي، من دون استضافة أو تنزيل الفيديو داخل التطبيق. |
| قائمة تشغيل | هيكل التنقل المستقبلي لاستعراض عناصر قائمة تشغيل مصدرها YouTube. |

## تخطيط الشاشة الرئيسية

يبدأ المحتوى بشريط علوي RTL يحتوي على اسم التطبيق وزر بحث وزر تحديث مستقل. يلي ذلك Header للقناة داخل بطاقة سطحية: مساحة صورة القناة، الاسم «أحياء مع عبدالرحمن الباز»، ووصف مختصر يشرح أن المحتوى التعليمي يُجلب من القناة الرسمية.

بعد الهوية، تظهر بطاقة حالة المصدر. إذا لم تتوفر بيانات الاتصال الرسمية، تعرض البطاقة رسالة «لم يتم تحميل محتوى القناة بعد» مع زر «إعداد مصدر YouTube»، دون أي عنوان أو صورة مصغرة مختلقة. عند نجاح التكامل مستقبلًا تتحول المنطقة تلقائيًا إلى قسم «أحدث الفيديوهات» مرتّبًا بالأحدث أولًا.

تحت ذلك توجد عناوين أقسام واضحة: «أحدث الفيديوهات»، «قوائم التشغيل»، و«المنهج». في المرحلة الحالية تعرض هذه الأقسام حالات فارغة تعليمية بدل عناصر وهمية. التمرير رأسي، والبطاقات مستقبلًا قابلة للمس وتستخدم صورة مصغرة بنسبة 16:9، عنوانًا محدود الأسطر، وتاريخ النشر القادم من YouTube.

## تدفقات الاستخدام الأساسية

### فتح التطبيق

يفتح المستخدم التطبيق → يرى هوية القناة والشرح → يرى حالة مصدر المحتوى بوضوح → يمكنه فتح إعدادات المصدر أو الضغط على تحديث.

### تحديث المحتوى

يضغط المستخدم على «تحديث» → يظهر مؤشر تحميل وتعطيل مؤقت للزر → تُستدعى طبقة YouTube الرسمية → تُحوّل الاستجابة إلى أنواع TypeScript مشتركة → تعرض الشاشة البيانات الحقيقية أو حالة خطأ مفهومة دون اختراع بدائل.

### إعداد المصدر

يضغط المستخدم «إعداد مصدر YouTube» → ينتقل إلى إعدادات المصدر → يرى Handle القناة الرسمي المقدم للمشروع ومكان Channel ID واسم متغير API الآمن → يحفظ الإعدادات المحلية غير السرية عند الحاجة فقط → يعود إلى الرئيسية.

### تشغيل فيديو مستقبلًا

يضغط المستخدم بطاقة فيديو حقيقية → شاشة التفاصيل تعرض بيانات YouTube → يضغط «مشاهدة على YouTube» → يفتح الرابط الرسمي عبر المتصفح/المشغل الرسمي المسموح به. لا يُنزّل ملف الفيديو ولا يُخزّن على سيرفر التطبيق.

## الألوان والطباعة

| الدور | اللون | الاستخدام |
|---|---|---|
| Primary | `#0B6E69` | الأزرار الرئيسية، الحالة النشطة، العلامات التعليمية |
| Primary Dark | `#07534F` | التباين في الوضع الداكن |
| Accent | `#F2B84B` | إبراز المنهج، مؤشرات التقدم، اللمسات الشبابية |
| Background | `#F7F9F8` | خلفية الشاشة الفاتحة |
| Surface | `#FFFFFF` | البطاقات والشرائح |
| Foreground | `#15302E` | النص الأساسي |
| Muted | `#6C7D7A` | النصوص الثانوية والحالات الفارغة |
| Border | `#DCE7E4` | الفواصل وحدود البطاقات |
| Error | `#B54747` | أخطاء الشبكة أو المصدر |

يُفضّل خط عربي نظامي واضح مع أوزان متدرجة: عنوان رئيسي 28، عناوين الأقسام 19، نص أساسي 16، وملاحظات 13–14، مع line-height مريح لتجنب قص الحروف العربية.

## قرارات النطاق

لا تُستخدم قاعدة بيانات، ولا Backend خاص، ولا تسجيل دخول، ولا رفع أو تنزيل للفيديوهات. لا تُضاف بيانات فيديو وهمية. تكامل YouTube يُجهّز كطبقة منفصلة قابلة للربط لاحقًا بالطريقة الرسمية، مع إبقاء Channel Handle المحدد في متطلبات المشروع وعدم اختراع Channel ID.

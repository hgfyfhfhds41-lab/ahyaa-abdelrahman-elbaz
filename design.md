# خطة تصميم UI/UX — «أحياء مع عبدالرحمن الباز»

## المرجع البصري

الصورة المرجعية المقدمة من المستخدم هي المصدر البصري الأساسي لهذه المرحلة. تم اعتماد روحها وتوزيعها: خلفية داكنة عميقة، هيدر هادئ بأيقونتي البحث والإشعارات، هوية الأستاذ في منطقة Hero، شريط اختصارات أفقي، بطاقات فيديو كبيرة يكون الـThumbnail فيها العنصر الأبرز، بطاقات قوائم تشغيل أفقية، وشريط تنقل سفلي ثابت بخمس وجهات.

التطبيق ليس نسخة من YouTube ولا Dashboard. الإحساس المستهدف هو «مساحة مذاكرة» شخصية، راقية ومريحة لطلاب الثانوية العامة، مع كثافة معلومات مضبوطة ومساحات واضحة بين المجموعات.

## Design System

| العنصر | القرار |
|---|---|
| الوضع | Dark Theme ثابت؛ الخلفية `#061719`، والسطوح `#112729` و`#172F31`. |
| اللون الأساسي | تركواز حيوي `#18C5B9` للأفعال والحالة النشطة. |
| لون الإبراز | ذهبي هادئ `#F1BB51` للعلامات التجريبية ولمسات المنهج. |
| النص | أبيض مائل للنعومة `#F4F8F7`، وثانوي `#8BA5A2`. |
| الحواف | زوايا 14–25px للبطاقات، دون تكديس مربعات صغير عشوائي. |
| اتجاه القراءة | RTL، مع ترتيب بصري يمين-إلى-يسار ونصوص عربية كاملة. |
| التفاعل | ضغط منخفض التباين، أزرار لمس كبيرة، وتنقل واضح بلا مسارات مسدودة. |

## الشاشات والتدفقات

| الشاشة | التكوين والوظيفة |
|---|---|
| الرئيسية | هيدر، Hero للأستاذ، أربع اختصارات، أحدث الفيديوهات، قوائم التشغيل، ووسم واضح بأن المحتوى تجريبي للعرض البصري. |
| الفيديوهات | عنوان فرعي للمكتبة، قائمة بطاقات فيديو كبيرة بصور Thumbnail تجريبية، مدة، مشاهدات، تاريخ، ومؤشر تقدم محلي تجريبي. |
| تفاصيل الفيديو | Thumbnail/منطقة تشغيل، العنوان، التصنيف، الإحصاءات، وصف الحالة التجريبية، وزر مشاهدة عبر YouTube لاحقًا. |
| قوائم التشغيل | Banner تعليمي وبطاقات أفقية تحمل اسم الباب وعدد الفيديوهات. |
| تفاصيل قائمة التشغيل | هوية الباب، عدد المقاطع، وقائمة فيديوهات متسقة. |
| البحث | حقل بحث بارز، نتائج Mock قابلة للتصفية، وحالة عدم وجود نتائج. |
| التنزيلات | واجهة «المشاهدة دون اتصال» بحالة فارغة؛ لا يوجد تنزيل فعلي في هذه المرحلة. |
| المزيد | معلومات التطبيق، رابط القناة الرسمية، وإعدادات مصدر YouTube. |
| إعدادات المصدر | Channel Handle ثابت للقناة الرسمية، مكان Channel ID، وتعليمات حفظ مفتاح API كسِرّ بيئي لاحقًا. |

## محتوى العرض التجريبي

تستخدم الشاشة مجموعة `MOCK_VIDEOS` و`MOCK_PLAYLISTS` المحلية بهدف إظهار الشكل البصري فقط. كل عنصر يحمل `isMock: true` ومعرّفًا يبدأ بـ`mock-`، وتظهر شارة `MOCK` على البطاقات ووسم توضيحي في الشاشة الرئيسية. عند بدء تكامل YouTube API لاحقًا، تُستبدل هذه المجموعة باستجابات القناة الرسمية دون تغيير بنية الواجهات.

## القيود المقصودة

لا توجد Firebase أو Supabase أو قاعدة بيانات أو Backend أو تسجيل دخول. لا يبدأ تكامل YouTube API في هذه المرحلة. لا يتم تنزيل أو تخزين أو استضافة أي ملف فيديو. مسارات التفاصيل الحالية للعرض البصري فقط، وزر المشاهدة مؤجل إلى مرحلة التكامل الرسمي.
